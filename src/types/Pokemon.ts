export type Pokemon = {
  name: string;
  url: string;
  favorite: boolean;
};

export type PokemonDetail = {
  name: string;
  image: string;
  weight: string;
  height: string;
  types: string[];
  favorite: boolean;
};
